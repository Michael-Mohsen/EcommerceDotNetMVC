@using Ecommerce1.Models;
@model List<CartItem>

<h2>Cart</h2>
@*
@if (Model != null && Model.Count > 0)
{
    <table>
        <tr>
            <th>Product ID</th>
            <th>Product Name</th>
            <th>Price</th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ProductId</td>
                <td>@item.Quantity</td>
                <td>@item.Price</td>
                <td>
                    <form method="post" action="@Url.Action("RemoveFromCart", "Product")">
                        <input type="hidden" asp-action="RemoveFromCart" asp-controller="Products" asp-route-id="@item.CartItemId" />
                        <button type="submit">Remove</button>
                    </form>
                </td>
            </tr>
        }
    </table>

    <form method="post" action="@Url.Action("Checkout", "Cart")">
        <button type="submit">Checkout</button>
    </form>
}
else
{
    <h3>Your cart is empty.</h3>
}*@